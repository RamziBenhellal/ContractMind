server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/contractmind
    username: postgres
    password: yourpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

# Custom Settings für den KI-Service
ai-service:
  url: http://localhost:8000  # Adresse des Python FastAPI Dienstes